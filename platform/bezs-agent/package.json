{
  "name": "bezs-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-agent clinical voice system with streaming STT, diarization, and MLflow tracking",
  "scripts": {
    "dev": "uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "uvicorn api.main:app --host 0.0.0.0 --port 8000",
    "install": "uv venv && uv pip install -e .",
    "lock": "uv lock",
    "sync": "uv sync"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Yazhnimalan/bezs-agent.git"
  },
  "keywords": [
    "clinical-voice",
    "diarization",
    "speech-to-text",
    "text-to-speech",
    "mlflow",
    "fastapi",
    "websocket"
  ]
}